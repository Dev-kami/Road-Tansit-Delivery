import NavBar from "./NavBar";

const HomePage = () => {
  return (
    <section className="h-full w-full bg-home-image bg-cover bg-center">
      <NavBar />
    </section>
  );
};

export default HomePage;
